NEXT_PUBLIC_SUPABASE_URL=https://sxrppeuogdwbsjzvpixq.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
SUPABASE_URL=https://sxrppeuogdwbsjzvpixq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace_only_in_local_or_github_secrets
COLLEGE_BOARD_EQB_AUTHORIZED=false

# Set to true to make `pnpm dev` use the local Supabase stack instead of the
# hosted project above. `pnpm dev:local` and `pnpm dev --hosted` override it per
# run. In local mode the URL and key come from `supabase status`, so the values
# above are ignored.
SUPABASE_LOCAL=false
